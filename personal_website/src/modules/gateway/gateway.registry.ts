import { CMS_GATEWAT_REGISTRY } from './CMS/cms.registry';
import { GATEWAY_EMAIL_REGISTRY } from './mailer/email.registry';

export const GATEWAY_REGISTRY = {
  CMS: CMS_GATEWAT_REGISTRY,
  EMAIL: GATEWAY_EMAIL_REGISTRY,
};
