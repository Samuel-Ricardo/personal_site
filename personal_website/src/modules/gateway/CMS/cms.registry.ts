import { SANITY_CMS_GATEWAY_REGISTRY as SANITY } from './sanity/sanity.registry';
import { STATIC_CMS_GATEWAY_REGISTRY as STATIC } from './static/static.registry';

export const CMS_GATEWAT_REGISTRY = {
  SANITY,
  STATIC,
};
