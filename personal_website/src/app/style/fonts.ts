import { Fraunces, Inconsolata } from 'next/font/google';

export const FRAUNCES = Fraunces({ subsets: ['latin'], preload: true });
export const INCONSOLATA = Inconsolata({ subsets: ['latin'], preload: true });
