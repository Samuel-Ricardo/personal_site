import { ProjectsBackground } from '@/assets/images/background/project/projects.background';

export const ProjectShowRoom = async ({ id }: { id: string }) => {
  return (
    <div>
      <h1>Hi! :) This page is under development 🌱 {id}</h1>
    </div>
  );
};
